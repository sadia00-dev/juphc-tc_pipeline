Save your real submission evidence here, named exactly as the grader expects:

  04-docker-image           -> terminal output of `docker run` showing tax-calculator on port 8080
  05-docker-icr-push        -> terminal output of the `docker push` to IBM Cloud Registry
  06-deployed-on-cloud      -> terminal output showing the app deployed on IBM Cloud (ibmcloud ce app get / kubectl get pods)
  10-final-output.png       -> screenshot of the app running, built by the Tekton pipeline

These must be YOUR real output/screenshots from running the commands in README.md.
